import Meta from "../utils/Meta";import {useDispatch, useSelector} from "react-redux";import {useState} from "react";import TodoItem from "../Components/TodoItem";import {addTodo} from './../Store/todoSlice'import {Divider, List} from "antd";import {CalendarOutlined} from "@ant-design/icons";export default function Todo() {  const data=useSelector(state=>state.todos.todos)  const dispatch=useDispatch();  const [text,setText]=useState('');  console.log(data)  return <div  className="container">    <Meta title={"Todo"} description={'dadadada'}/>      <div className="todoApp">        <div className="input_send">          <label>            <div className="circle"></div>        <input placeholder="Task" className="input_head" type="text" onChange={(e)=>{setText(e.target.value)}} value={text}/>          </label>          <CalendarOutlined className="icon_group" />            <button onClick={()=>dispatch(addTodo(text))} >Add</button>        </div>        <div className="todo_list">          {/*<Divider orientation="left">Large Size</Divider>*/}          <List            size="large"            bordered={false}            dataSource={data}            renderItem={(elem) => <List.Item>{<TodoItem key={elem.id} id={elem.id} done={elem.done} text={elem.text} />}</List.Item>}          />        </div>      </div>    </div>};