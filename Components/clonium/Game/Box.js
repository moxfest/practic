import React, {FC} from 'react';import Square from "./Square";import s from '../../styles/Box.module.scss'const Box:FC<any> = (props) => {    return (        <div className={s.box}>            {props.data.map((elem:any, index:any) => {             return <Square clickFunc={()=>{props.clickFunc(index)}} value={elem} key={index} step={props.step} />   //TODO: подумать над другим ключом            })            }        </div>    );};export default Box;