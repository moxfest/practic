import React from 'react';import s from '../../../styles/Box.module.scss'const Square= ({value,...props}) => {const color=['white','red','blue','purple','yellow']    const dotArr=new Array(value.value).fill(1);const step=props.step==value.player? ' activeStep':'';    return (        <>            {                <button className={s.square+' '+step } onClick={props.clickFunc} disabled={(value.value===null || value.value === 0 || props.step==false && value.player==1 || props.step!=value.player)  && true}>                <div className={color[value.player]+' '+s.btnSquare} >                    {dotArr.map(()=>{                        return (<div className={s.dote}></div>);                    })                    }                </div>                </button>            }        </>    );};export default Square;