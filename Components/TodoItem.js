import React from 'react';import {useDispatch} from "react-redux";import {removeTodo,doneToggle} from './../Store/todoSlice'import {Checkbox} from "antd";import {DeleteOutlined} from "@ant-design/icons";const TodoItem = (props) => {  console.log(props)  const dispatch=useDispatch();  return (<>     <div> <Checkbox  id="element" checked={props.done} onChange={() => dispatch(doneToggle(props.id))}></Checkbox><span className="text_task">{props.text}</span></div>      <span className="span_delete" onClick={() => {        dispatch(removeTodo(props.id))      }}><DeleteOutlined className={'delete_icon'}/></span>    </>  );};export default TodoItem;